<% if @user.role == 1 %>
<div class="container-fluid">
    <ul class="nav nav-tabs"> 
        <li class="active dash-li"><a href="#firsttab" data-toggle="tab">My Quizzes</a></li>
        <li class="dash-li"><a href="#secondtab" data-toggle="tab">Attempted Quizzes</a></li>
        <li class="dash-li"><a href="#thirdtab" data-toggle="tab">All Quizzes</a></li>
    </ul>
    <div class="tab-content">
        <div class="tab-pane fade in active" id="firsttab">
                <div class="list_quizzes">
                    <% @created_quizzes.each do |quiz| %>
                       <div class="card my-2">
                            <div class="card-body quiz-card">
                                <div class="card-title">
                                    <%= link_to quiz.title, quiz_path(quiz) %> <br />
                                    Points:  <%= quiz.points %>
                                </div>
                            </div>
                        </div>
                    <% end %>
                </div>
        </div>
        <div class="tab-pane fade" id="secondtab">
            <%#<a class="nav-link active" href="#" data-toggle="tab"><h1 class="display-6">My Quizzes</h1></a> %>
                <div class="list_quizzes">
                    <% @attempted_quizzes.each do |quiz| %>
                       <div class="card my-2">
                            <div class="card-body quiz-card">
                                <div class="card-title">
                                    <%= link_to quiz.title, quiz_path(quiz) %> <br />
                                    Points:  <%= quiz.points %>
                                </div>
                            </div>
                        </div>
                    <% end %>
                </div>
        </div>
        <div class="tab-pane fade" id="thirdtab">
            <%#<a class="nav-link active" href="#" data-toggle="tab"><h1 class="display-6">My Quizzes</h1></a> %>
                <div class="list_quizzes">
                    <% @quizzes.each do |quiz| %>
                       <div class="card my-2">
                            <div class="card-body quiz-card">
                                <div class="card-title">
                                    <%= link_to quiz.title, quiz_path(quiz) %> <br />
                                    Points:  <%= quiz.points %>
                                </div>
                            </div>
                        </div>
                    <% end %>
                </div>
        </div>
    </div>
</div>
<% end %>

<% if @user.role == 2 %>

<div class="container-fluid">
    <ul class="nav nav-tabs"> 
          <li><a href="#firstab" data-toggle="tab">Attempted Quizzes</a></li>
          <li><a href="#secondtab" data-toggle="tab">All Quizzes</a></li>
    </ul>

    <div class="tab-content">
        <div class="tab-pane active" id="firsttab">
            <%#<a class="nav-link active" href="#" data-toggle="tab"><h1 class="display-6">My Quizzes</h1></a> %>
                <div class="list_quizzes">
                    <% @attempted_quizzes.each do |quiz| %>
                       <div class="card my-2">
                            <div class="card-body quiz-card">
                                <div class="card-title">
                                    <%= link_to quiz.title, quiz_path(quiz) %> <br />
                                    Points:  <%= quiz.points %>
                                </div>
                            </div>
                        </div>
                    <% end %>
                </div>
        </div>
        <div class="tab-pane fade" id="secondtab">
            <%#<a class="nav-link active" href="#" data-toggle="tab"><h1 class="display-6">My Quizzes</h1></a> %>
                <div class="list_quizzes">
                    <% @quizzes.each do |quiz| %>
                       <div class="card my-2">
                            <div class="card-body quiz-card">
                                <div class="card-title">
                                    <%= link_to quiz.title, quiz_path(quiz) %> <br />
                                    Points:  <%= quiz.points %>
                                </div>
                            </div>
                        </div>
                    <% end %>
                </div>
        </div>
    </div>
 </div>
<% end %>

